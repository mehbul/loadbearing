# skills/ — _Phase 4_

The workflows. Each is a folder with a `SKILL.md` (YAML frontmatter + steps),
optional `scripts/` (runnable, pass/fail) and `references/`.

Planned skills:
- `define-perf-targets` — turn "10k users" into p95 / error-rate / RPS targets + capacity math
- `run-load-test` — k6 runner whose thresholds set the exit code
- `diagnose-bottlenecks`
- `add-observability`
- `perf-readiness-review` — tiered: ready for 100 / 1k / 10k / 100k
- `write-perf-report` — fixed, honest report template

_This is a placeholder; content lands in Phase 4._
